{
  "name": "choke-hole-no-holes-barred",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "CHOKE HOLE: NO HOLES BARRED — Extreme Drag Wrestling. Stylised 3D mobile arcade wrestling PWA.",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke-test.mjs",
    "pace": "node scripts/pace.mjs",
    "check-deploy": "node scripts/check-deploy.mjs"
  },
  "dependencies": {
    "@babylonjs/core": "^8.56.2"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "playwright": "^1.63.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "vite-plugin-pwa": "^1.0.3",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
